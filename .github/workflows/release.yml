name: Release

# Single linear release flow. The KEY invariant: the PHAR binary (composer `bin`,
# builds/repos) is built and committed to main BEFORE the tag is created, and the
# tag is pinned to that exact commit. Nothing rebuilds onto a post-tag commit, so the
# tag never moves and Composer's stable-version immutability is never violated.

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Explicit version (e.g. 0.3.0). Leave blank to auto-bump the patch.'
        required: false

permissions: write-all

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: main
          fetch-depth: 0

      - name: Resolve version
        id: v
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            NEXT="${{ inputs.version }}"
            NEXT="${NEXT#v}"
          else
            LATEST=$(git tag -l \
              | sed 's/^v//' \
              | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' \
              | sort -V \
              | tail -n1)
            [ -z "$LATEST" ] && LATEST="0.0.0"
            IFS='.' read -r MA MI PA <<< "$LATEST"
            NEXT="${MA}.${MI}.$((PA + 1))"
          fi
          # Never reuse a version that already shipped as a release.
          while gh release view "$NEXT" &>/dev/null; do
            IFS='.' read -r MA MI PA <<< "$NEXT"
            NEXT="${MA}.${MI}.$((PA + 1))"
          done
          echo "version=$NEXT" >> "$GITHUB_OUTPUT"
          echo "Releasing $NEXT"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2.35.5
        with:
          php-version: 8.4
          extensions: dom, curl, libxml, mbstring, zip
          ini-values: error_reporting=E_ALL, phar.readonly=Off
          tools: composer:v2
          coverage: none

      - name: Install the dependencies
        run: composer install --prefer-dist --optimize-autoloader --no-interaction --no-progress --no-suggest --no-scripts --ansi -v --ignore-platform-req=ext-pcntl --ignore-platform-req=ext-posix --ignore-platform-req=ext-sockets

      - name: Stamp embedded version
        run: echo "${{ steps.v.outputs.version }}" > version.txt

      - name: Generate CHANGELOG.md
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5 # v4.8.0
        with:
          config: cliff.toml
          args: --tag ${{ steps.v.outputs.version }} -o CHANGELOG.md

      - name: Generate release notes
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5 # v4.8.0
        with:
          config: cliff.toml
          args: --tag ${{ steps.v.outputs.version }} --unreleased --strip all -o RELEASE_NOTES.md

      - name: Build the PHAR with the release version
        run: php repos app:build repos --build-version=${{ steps.v.outputs.version }}

      - name: Commit release artifacts to main
        id: commit
        uses: stefanzweifel/git-auto-commit-action@4a55954c782fc1ea30b9056cd3e7a2b40ca8887d # v7
        with:
          branch: main
          commit_message: "Release ${{ steps.v.outputs.version }}"
          file_pattern: "builds/repos version.txt CHANGELOG.md"

      - name: Resolve release commit
        id: sha
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      # Tag is pinned to the commit that ALREADY contains the built binary.
      - name: Create tag and release at the release commit
        run: |
          gh release create ${{ steps.v.outputs.version }} \
            --title "${{ steps.v.outputs.version }}" \
            --notes-file RELEASE_NOTES.md \
            --target ${{ steps.sha.outputs.sha }} \
            --latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # self-update (SelfUpdateCommand::assetName) fetches the "repos.phar" asset.
      # The committed binary IS the PHAR; upload it here. We can't rely on a separate
      # "release: created" workflow: releases created by GITHUB_TOKEN do not fire that
      # event (GitHub anti-recursion), so the asset must be uploaded in this same job.
      - name: Upload the PHAR asset
        run: |
          cp builds/repos repos.phar
          gh release upload ${{ steps.v.outputs.version }} repos.phar --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
